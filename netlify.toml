[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundle = "all"

# Configure environment variable overrides for production if needed
[build.environment]
  NODE_VERSION = "20"
